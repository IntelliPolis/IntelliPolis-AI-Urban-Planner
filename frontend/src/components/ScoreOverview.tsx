import { Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";
import type { CityScores } from "../types/city";
import { score } from "../utils/format";
export default function ScoreOverview({scores}:{scores:CityScores|null}){if(!scores)return <section className="card"><h2>현재 도시 점수</h2><p className="empty">점수 정보가 없습니다.</p></section>;const data=[["교통",scores.traffic],["환경",scores.environment],["경제",scores.economy],["생활",scores.living],["종합",scores.overall]].map(([name,value])=>({name,value}));return <section className="card"><h2>현재 도시 점수</h2><div className="score-cards">{data.map(x=><div key={x.name}><span>{x.name}</span><strong>{score(Number(x.value))}</strong></div>)}</div><div className="chart"><ResponsiveContainer><BarChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis domain={[0,100]}/><Tooltip/><Bar dataKey="value" name="점수" fill="#168aad" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div></section>}
