import type { AgentAnalysis } from "../types/city";
import { domainLabel,score,severityLabel } from "../utils/format";
export default function AgentAnalysisPanel({items}:{items:AgentAnalysis[]}){return <section><h2>분야별 AI 분석</h2>{!items.length?<p className="card empty">분석 결과가 없습니다.</p>:<div className="agents">{items.map(a=><details className={`card domain-${a.domain.toLowerCase()}`} key={a.domain} open><summary><strong>{domainLabel[a.domain]}</strong><span>{score(a.score)}</span></summary><p>{a.summary}</p>{a.problems.map((p,i)=><div className="analysis-item" key={i}><b>문제점 · 심각도 {severityLabel[p.severity]}</b><strong>{p.title}</strong><p>{p.description}</p><small>판단 근거: {p.evidence}</small></div>)}{a.suggestions.map((s,i)=><div className="analysis-item" key={i}><b>개선 제안 · 비용 {severityLabel[s.estimatedCostLevel]}</b><strong>{s.title}</strong><p>{s.description}</p><small>예상 효과: {s.expectedEffect}</small></div>)}{a.warnings?.map((w,i)=><p className="inline-warning" key={i}>{w}</p>)}</details>)}</div>}</section>}
