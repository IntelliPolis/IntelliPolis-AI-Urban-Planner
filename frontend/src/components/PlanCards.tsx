import type { CityPlan,PlanType } from "../types/city";
import { money,planLabel,score } from "../utils/format";
export default function PlanCards({plans,selected,onSelect}:{plans:CityPlan[];selected:PlanType|null;onSelect:(p:CityPlan)=>void}){return <section><h2>도시계획 대안</h2><div className="plans">{plans.map(p=><article className={`card plan ${selected===p.planType?"selected":""}`} key={p.planType}><div className="eyebrow">{planLabel[p.planType]} {selected===p.planType&&"· 선택됨"}</div><h3>{p.name}</h3><p>{p.summary}</p><dl><div><dt>예상 예산</dt><dd>{money(p.estimatedCost)}</dd></div><div><dt>예상 종합 점수</dt><dd>{score(p.expectedScores?.overall)}</dd></div><div><dt>시설 / 교통 / 구역</dt><dd>{p.facilities.length} / {p.roads.length} / {p.zones.length}</dd></div></dl><details><summary>장점과 실행 계획</summary><ul>{p.benefits.map(x=><li key={x}>{x}</li>)}</ul><b>Trade-off</b><ul>{p.tradeOffs.map(x=><li key={x}>{x}</li>)}</ul>{p.phases.map(x=><p key={x.order}><b>{x.order}단계 {x.name}</b> — {x.description}</p>)}</details><button onClick={()=>onSelect(p)}>지도에서 보기</button></article>)}</div></section>}
