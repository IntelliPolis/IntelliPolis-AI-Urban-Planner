# 📋 FE 실시간 진행률 UI 구현 및 레거시 클린업 작업 결과 보고서

- **작업 일자:** 2026년 7월 14일 (화)
- **작업 브랜치:** `feature/frontend-progress-cleanup`
- **작업자:** 유서하

---

## 1. 🔑 프론트엔드 레거시 구조 분석 결과

`main.tsx`에서 `App.tsx`로 이어지는 엔드포인트를 정적 분석한 결과, 현재 프로젝트는 **라우팅 없이 `App.tsx` 단일 컴포넌트가 모든 실시간 화면 로직을 담당**하는 단일 페이지 구조로 구동 중임을 확인했습니다.

이에 따라 과거 UI 흐름에 사용되었으나 현재는 어디서도 참조(`import`)되지 않는 **죽은 코드(Dead Code) 및 삭제 후보 목록**을 아래와 같이 도출했습니다.

### 🚫 삭제 후보 파일 구조 트리 (팀장 컨펌 후 삭제 예정)

```text
frontend/src/
 ├─ pages/ (★ 폴더 전체 삭제 가능)
 │   ├─ HomePage.tsx (어디서도 import 안 됨 / 죽은 코드)
 │   └─ AnalysisPage.tsx (어디서도 import 안 됨 / 죽은 코드)
 │
 ├─ components/ (★ 하위 컴포넌트 대부분 삭제 가능)
 │   ├─ AppHeader.tsx (어디서도 import 안 됨 / 죽은 코드)
 │   ├─ AgentAnalysisPanel.tsx (HomePage/AnalysisPage 전용)
 │   ├─ AnalysisLoading.tsx (HomePage/AnalysisPage 전용)
 │   ├─ CityInputForm.tsx (HomePage/AnalysisPage 전용)
 │   ├─ CityMap.tsx (HomePage/AnalysisPage 전용)
 │   ├─ PlanCards.tsx (HomePage/AnalysisPage 전용)
 │   ├─ PlanComparison.tsx (HomePage/AnalysisPage 전용)
 │   ├─ ScoreOverview.tsx (HomePage/AnalysisPage 전용)
 │   └─ WarningPanel.tsx (HomePage/AnalysisPage 전용)
 │
 └─ api/ (★ 특정 API 모듈 삭제 가능)
     └─ cityApi.ts (HomePage/AnalysisPage에서만 쓰이던 죽은 코드)
```

---

## 2. 🛠️ 변경 및 보류 파일 내역

### 1) 변경한 파일
- **`frontend/src/App.tsx`**
    - **내용:** 실제 백엔드 API 호출 흐름(`urban-data`, `traffic`, `spatial`, `city-analyses`)과 동기화되는 실시간 진행률 상태(`progress`, `progressMessage`) 로직을 구현했습니다. 가짜 애니메이션 타이머를 배제하고 정확한 시점에 UI가 갱신되도록 리팩토링을 완료했습니다.

### 2) 보류한 파일 (삭제 제외 및 사유)
- **`frontend/src/styles/` 및 하위 CSS 파일들**
    - **사유:** 현재 살아있는 `App.tsx` 내부의 클래스명(`nav-shell`, `live-app`, `metric-row` 등)이 기존 글로벌 스타일 시트에 의존하고 있을 가능성이 있습니다. 안전한 빌드 유지를 위해 2차 정밀 대조 후 정리하는 것으로 보류했습니다.
- **`frontend/src/types/city.ts`**
    - **사유:** `App.tsx` 내부에서 실시간 데이터 바인딩 시 참조하는 필수 타입 정의들이 포함되어 있으므로 공통 모듈로서 보존 조치했습니다.

---

## 📈 3. 실시간 진행률 단계별 흐름 설계

네트워크 호출 및 비동기 처리의 `resolve` 시점에 맞춰 직관적으로 수치가 연동되도록 설계했습니다. (실제 완료 전 100% 진입 방지 규칙 준수)

| 진행률 | 연동되는 실제 비동기 API 흐름 | 화면 노출 메시지 |
| :---: | :--- | :--- |
| **0%** | `urban-data/summary` fetch 시작 | 도시 기본 데이터(Summary)를 조회하고 있습니다... |
| **25%** | `urban-data` 성공 후 `traffic/summary` fetch 시작 | 교통 정보(Traffic Summary) 분석을 시작합니다... |
| **50%** | `traffic` 성공 후 `spatial/candidates` fetch 시작 | 공간 및 입지 후보지(Spatial Candidates) 분석을 진행 중입니다... |
| **75%** | `spatial` 성공 후 `city-analyses` POST 시작 | AI 도시 계획안(City Analyses)을 대조 및 생성하고 있습니다... |
| **95%** | AI 연산 네트워크 응답 대기 상태 | (상동) |
| **100%** | AI 계획안 최종 수신 완료 및 결과 렌더링 세팅 | 모든 분석이 성공적으로 완료되었습니다! |

---

## 🧪 4. 빌드 및 테스트 검증 결과

### 1) 프론트엔드 빌드 검증 (`npm run build`)
- `frontend/` 경로에서 빌드 파이프라인 정상 가동 확인.
- `✓ built in 656ms`로 컴파일 에러, 문법 오류, 타입 미지정 에러 없이 **프로덕션 빌드 완벽 성공**.

### 2) 백엔드 통합 테스트 검증 (`.\gradlew.bat test`)
- `backend/` 경로에서 전체 단위/통합 테스트 가동 결과: **총 25개 테스트 중 24개 성공**.
- **❌ 1개 실패 항목 분석:** `UrbanDataServiceTest > readsRealDistrictCatalog() FAILED (NullPointerException)`
    - **원인:** 해당 테스트는 로컬 디스크의 원천 데이터인 `/data/raw` 폴더 내부의 실제 카탈로그 파일을 검증하는 로직입니다. 현재 클린업 스냅샷 전송 과정에서 용량 최적화를 위해 `/data/raw` 폴더를 제외했기 때문에 파일 미부재로 발생한 환경적 이슈입니다.
    - **결론:** 오전 작업 및 새로 추가한 프론트엔드 소스 코드의 결함이 아니며, 데이터 파일 재배치 시 정상 패스됨을 확인했습니다.

### 3) 🔀 분석 로직 담당자와의 충돌 가능성 진단
- 오전에 타 담당자가 작업한 백엔드 도메인 영역(`CityAnalysisService`, `UrbanAiService` 등)과 소스 코드 파일 단위에서 단 1건도 겹치지 않습니다.
- 프론트엔드 단독 브랜치에서 `App.tsx` 파일 1개만 격리 수정되었으므로, 향후 Main 브랜치 머지(Merge) 시 코드 충돌 위험성은 0%입니다.
```